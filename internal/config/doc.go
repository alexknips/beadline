// Package config reads beadline.toml: the repositories, and an [expert]
// table for the conventions (roadmap types, goal labels, human gates,
// ignored types) and the model settings, with defaults and validation. One
// repository needs no file at all. See docs/design.md, ADR-2 §5.
package config
