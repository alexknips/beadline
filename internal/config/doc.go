// Package config reads beadline.toml: repositories, conventions (high-level
// types, goal labels, human-gate patterns, infra types) and model settings,
// with defaults and validation. See docs/design.md, "Inputs".
package config
