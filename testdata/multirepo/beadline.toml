# Synthetic three-repo fixture: see ../README.md.
repos = ["api.jsonl", "web.jsonl", "hq.jsonl"]

[expert]
agents = { api = 3, hq = 1 }
human_gate = { metadata = ["awaiting_signoff", "hold_reason"] }
